import { createTheme } from '@mui/material/styles';

const theme = createTheme({
  palette: {
    primary: {
      light: '#AECCCD',
      main: '#5F9EA0',
      dark: '#219195',
      contrastText: '#fff',
    },
  },
});

export default theme;
